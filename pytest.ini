[pytest]
testpaths = tests
addopts = -ra -q --strict-markers --strict-config
markers =
    slow: marks tests as slow
    integration: integration tests (FastAPI TestClient, real ffmpeg, etc.)
    gpu: requires real CUDA device
